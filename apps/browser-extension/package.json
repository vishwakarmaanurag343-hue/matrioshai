{
  "name": "matrioshai-browser-extension",
  "version": "0.1.0",
  "description": "MATRIOSHAI Unified Personal AI Operating System — Chrome Extension Foundation (Phase 1)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "node scripts/generate-icons.js && tsc && vite build",
    "check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/node": "^20.11.24",
    "happy-dom": "^20.11.6",
    "typescript": "^5.2.2",
    "vite": "^5.1.6",
    "vite-plugin-static-copy": "^1.0.6",
    "vitest": "^1.3.1"
  }
}
